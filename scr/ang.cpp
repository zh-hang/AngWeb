#include <sys/types.h>
#include <sys/socket.h>

int main(){
    int listenfd,connfd;
    socket_t clientlen;
    struct sockaddr_storage clientaddr;
    char client_hostname[MAXHOSTNAME],client_port[MAXHOSTNAME];

    

    return 0;
}